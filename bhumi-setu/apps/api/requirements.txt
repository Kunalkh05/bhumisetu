# BHUMISETU API — runtime dependencies.
# Stack fixed by design §3 / "Language and stack": Python 3.11, FastAPI,
# SQLAlchemy 2.0, Alembic, Celery on Redis, PostgreSQL 15 + PostGIS 3.3.
# Versions are pinned exactly; the same pins are used by the workers so that
# api and workers import identical service modules (§3.3).

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
# The citizen surface is server-rendered HTML from FastAPI, not an SPA (§10.2):
# the transfer budget of R24.1 rules out shipping React. Jinja2 renders the
# response body the gate has already redacted (§8.2, task 5.3).
Jinja2==3.1.6
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
celery[redis]==5.4.0
redis==5.2.1

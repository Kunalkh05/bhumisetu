import { PagePlaceholder } from './PagePlaceholder';

export function MapPage() {
  return (
    <PagePlaceholder
      title="Map"
      task="18.4"
      requirements="16.1, 16.4, 16.5, 16.6"
      note="Clusters above the configured threshold rather than rendering every parcel; unscored parcels take their own shade with a labelled legend row."
    />
  );
}

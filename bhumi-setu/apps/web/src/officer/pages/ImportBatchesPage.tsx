import { PagePlaceholder } from './PagePlaceholder';

export function ImportBatchesPage() {
  return (
    <PagePlaceholder
      title="Import batches"
      task="26.4"
      requirements="30.13"
      note="Rejected rows are filterable by failing rule, since a batch report with 4000 rejections is unusable unsorted."
    />
  );
}

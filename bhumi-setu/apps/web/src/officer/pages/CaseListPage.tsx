import { PagePlaceholder } from './PagePlaceholder';

export function CaseListPage() {
  return (
    <PagePlaceholder
      title="Cases"
      task="8.3"
      requirements="2.2, 2.3"
      note="Scoped to the officer's jurisdiction; an out-of-scope case returns no attributes at all."
    />
  );
}

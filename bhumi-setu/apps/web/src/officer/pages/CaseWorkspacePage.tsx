import { PagePlaceholder } from './PagePlaceholder';

export function CaseWorkspacePage() {
  return (
    <PagePlaceholder
      title="Case workspace"
      task="18.2"
      requirements="23.1, 23.5"
      note="Renders three distinct risk states from explicit response fields — scored, not-scored, and scored-but-unmonitored — never inferred from a null."
    />
  );
}
